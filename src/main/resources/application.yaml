
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/clidb
    username: cliuser
    password: veryverysecretword
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate.ddl-auto: update
    show-sql: false
    properties.hibernate.format_sql: false


server:
  port: 8080

